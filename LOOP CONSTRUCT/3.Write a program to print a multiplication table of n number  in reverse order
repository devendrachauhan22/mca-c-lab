#include <stdio.h>

int main() {
    int n, i;

    // Input the number from the user
    printf("Enter a number: ");
    scanf("%d", &n);

    // Print the multiplication table in reverse order
    for (i = 10; i >= 1; i--) {
        printf("%d x %d = %d\n", n, i, n * i);
    }

    return 0;
}
